{
  "name": "instagram",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup-production": "npm run build-client && npm run build-server",
    "build-client": "cd client && npm install &&  npm run build",
    "build-server": "npm install && pip install -r requirements.txt",
    "server": "python3 manage.py runserver",
    "client": "npm start --prefix client",
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
